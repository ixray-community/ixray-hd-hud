function smoke_give_bandage(first, second)

	local npc = first
	if first ~= nil and first:id() == db.actor:id() then
		npc = second
	end
	if npc == nil then
		return
	end
	dialogs.relocate_item_section(npc, "bandage", "in", 1)
end


